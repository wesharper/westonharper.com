---
import HeaderLink from "./HeaderLink.astro";

export interface Props {
  headerClass?: string;
}

const { headerClass } = Astro.props;
---

<header
  class={`sticky top-0 left-0 right-0 flex items-center justify-end gap-x-5 px-7 md:px-10 py-6 bg-slate-900 bg-opacity-80 ${
    headerClass ?? ""
  }`}
>
  <nav class="flex gap-x-5 text-slate-800 dark:text-slate-100">
    <HeaderLink href="/">Home</HeaderLink>
    <HeaderLink href="/blog">Blog</HeaderLink>
  </nav>
</header>
