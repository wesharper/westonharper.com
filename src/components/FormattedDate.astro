---
type Props = {
  date: Date;
};

const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="dark:text-slate-400">
  {
    date.toLocaleDateString("en-us", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
